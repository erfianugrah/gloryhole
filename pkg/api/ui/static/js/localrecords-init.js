/**
 * Local Records Page Initialization
 * Imports and initializes local records page functionality
 */

import { initializeLocalRecords } from './modules/localrecords.js';

// Initialize when DOM is ready
document.addEventListener('DOMContentLoaded', function() {
    initializeLocalRecords();
});

/**
 * Clients Page Initialization
 * Imports and initializes clients page functionality
 */

import { initializeClients } from './modules/clients.js';

// Initialize when DOM is ready
document.addEventListener('DOMContentLoaded', function() {
    initializeClients();
});

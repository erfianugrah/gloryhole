# Design Documents

This directory contains active design proposals and architectural documents for Glory-Hole.

Completed and historical designs have been archived. See git history for previous iterations.

## Process

1. Create a design doc as `<feature-name>.md` in this directory
2. Reference it in a PR for discussion
3. Move to implementation once approved
4. Remove the design doc once the feature ships (the code is the source of truth)
